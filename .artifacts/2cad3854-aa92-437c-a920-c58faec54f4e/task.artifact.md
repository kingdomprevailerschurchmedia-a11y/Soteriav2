# Task: Fix Compilation Errors

- `[ ]` Run code generation (`build_runner`)
- `[ ]` Address remaining type mismatch errors (`num` to `int`)
- `[ ]` Fix logic errors and missing return values
- `[ ]` Verify build compiles
- `[ ]` Run unit tests (if applicable)
