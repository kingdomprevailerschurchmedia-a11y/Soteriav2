# Task: Rename Package Name to `com.soteria.app`

- `[ ]` Update Android `build.gradle.kts` (namespace and applicationId)
- `[ ]` Update `MainActivity.kt` package declaration
- `[ ]` Move `MainActivity.kt` to new directory structure
- `[ ]` Update iOS `project.pbxproj` bundle identifiers
- `[ ]` Run `flutter analyze` and verify build
